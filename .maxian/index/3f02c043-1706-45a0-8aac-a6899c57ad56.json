{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/editor/common/languageFeatureRegistry.js","code":"i(model, recursive) {\n        const notebookInfo = this.f?.(model.uri);\n        // use the uri (scheme, pattern) of the notebook info iff we have one\n        // otherwise it's the model's/document's uri\n        const candidate = notebookInfo\n            ? new MatchCandidate(model.uri, model.getLanguageId(), notebookInfo.uri, notebookInfo.type, recursive)\n            : new MatchCandidate(model.uri, model.getLanguageId(), undefined, undefined, recursive);\n        if (this.h?.equals(candidate)) {\n            // nothing has changed\n            return;\n        }\n        this.h = candidate;\n        for (const entry of this.d) {\n            entry._score = $zE(entry.selector, candidate.uri, candidate.languageId, $sG(model), candidate.notebookUri, candidate.notebookType);\n            if (isExclusive(entry.selector) && entry._score > 0) {\n                if (recursive) {\n                    entry._score = 0;\n                }\n                else {\n                    // support for one exclusiv","startLine":115,"endLine":146,"chunkType":"function","mtime":1772878380560.9429}