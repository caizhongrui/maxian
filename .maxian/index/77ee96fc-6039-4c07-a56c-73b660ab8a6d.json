{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/base/browser/touch.js","code":"}\n            else if (activeTouchCount === 1) {\n                const finalX = arrays.$xb(data.rollingPageX);\n                const finalY = arrays.$xb(data.rollingPageY);\n                const deltaT = arrays.$xb(data.rollingTimestamps) - data.rollingTimestamps[0];\n                const deltaX = finalX - data.rollingPageX[0];\n                const deltaY = finalY - data.rollingPageY[0];\n                // We need to get all the dispatch targets on the start of the inertia event\n                const dispatchTo = [...this.j].filter(t => data.initialTarget instanceof Node && t.contains(data.initialTarget));\n                this.F(targetWindow, dispatchTo, timestamp, // time now\n                Math.abs(deltaX) / deltaT, // speed\n                deltaX > 0 ? 1 : -1, // x direction\n                finalX, // x now\n                Math.abs(deltaY) / deltaT, // y speed\n                deltaY > 0 ? 1 : -1, // y direction\n                finalY // y now\n                );\n            }\n     ","startLine":130,"endLine":154,"chunkType":"block","mtime":1772878377885.3826}