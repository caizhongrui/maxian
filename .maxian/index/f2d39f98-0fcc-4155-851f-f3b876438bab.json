{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/base/node/processes.js","code":"async function findExecutable(command, cwd, paths) {\n        // If we have an absolute path then we take it.\n        if (path.$mc(command)) {\n            return command;\n        }\n        if (cwd === undefined) {\n            cwd = process.cwd();\n        }\n        const dir = path.$qc(command);\n        if (dir !== '.') {\n            // We have a directory and the directory is relative (see above). Make the path absolute\n            // to the current working directory.\n            return path.$nc(cwd, command);\n        }\n        if (paths === undefined && Types.$yg(process.env['PATH'])) {\n            paths = process.env['PATH'].split(path.$xc);\n        }\n        // No PATH environment. Make path absolute to the cwd.\n        if (paths === undefined || paths.length === 0) {\n            return path.$nc(cwd, command);\n        }\n        async function fileExists(path) {\n            if (await pfs.Promises.exists(path)) {\n                let statValue;\n                try {","startLine":49,"endLine":73,"chunkType":"block","mtime":1772878378445.782}