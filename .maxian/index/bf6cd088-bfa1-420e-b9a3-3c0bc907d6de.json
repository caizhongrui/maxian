{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/base/browser/ui/list/listView.js","code":"}\n        else {\n            this.g.splice(start, deleteCount, inserted);\n            deleted = this.d.splice(start, deleteCount, ...inserted);\n        }\n        const delta = elements.length - deleteCount;\n        const renderRange = this.vb(this.k, this.m);\n        const renderedRestRange = $ZZ(previousRenderedRestRange, delta);\n        const updateRange = Range.intersect(renderRange, renderedRestRange);\n        for (let i = updateRange.start; i < updateRange.end; i++) {\n            this.bb(this.d[i], i);\n        }\n        const removeRanges = Range.relativeComplement(renderedRestRange, renderRange);\n        for (const range of removeRanges) {\n            for (let i = range.start; i < range.end; i++) {\n                this.db(i);\n            }\n        }\n        const unrenderedRestRanges = previousUnrenderedRestRanges.map(r => $ZZ(r, delta));\n        const elementsRange = { start, end: start + elements.length };\n        const insertRanges = [elementsRange, ...unrenderedRestRanges].ma","startLine":413,"endLine":437,"chunkType":"block","mtime":1772878378009.5977}