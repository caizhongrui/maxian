{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/base/browser/ui/splitview/splitview.js","code":"L({ sash, start, alt }) {\n        for (const item of this.n) {\n            item.enabled = false;\n        }\n        const index = this.sashItems.findIndex(item => item.sash === sash);\n        // This way, we can press Alt while we resize a sash, macOS style!\n        const disposable = $Zc($$i(this.el.ownerDocument.body, 'keydown', e => resetSashDragState(this.t.current, e.altKey)), $$i(this.el.ownerDocument.body, 'keyup', () => resetSashDragState(this.t.current, false)));\n        const resetSashDragState = (start, alt) => {\n            const sizes = this.n.map(i => i.size);\n            let minDelta = Number.NEGATIVE_INFINITY;\n            let maxDelta = Number.POSITIVE_INFINITY;\n            if (this.w) {\n                alt = !alt;\n            }\n            if (alt) {\n                // When we're using the last sash with Alt, we're resizing\n                // the view to the left/up, instead of right/down as usual\n                // Thus, we must do the inverse of the usual\n            ","startLine":529,"endLine":553,"chunkType":"block","mtime":1772878378110.4668}