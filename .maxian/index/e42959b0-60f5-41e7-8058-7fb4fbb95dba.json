{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/base/browser/ui/inputbox/inputBox.js","code":"}\n            }\n        };\n        // Spot the change to the textarea class attribute which occurs when it changes between non-empty and empty,\n        // and add the history suffix to the placeholder if not yet present\n        this.db = new MutationObserver((mutationList, observer) => {\n            mutationList.forEach((mutation) => {\n                if (!mutation.target.textContent) {\n                    addSuffix();\n                }\n            });\n        });\n        this.db.observe(this.c, { attributeFilter: ['class'] });\n        this.D(this.c, () => addSuffix());\n        this.C(this.c, () => {\n            const resetPlaceholder = (historyHint) => {\n                if (!this.r.endsWith(historyHint)) {\n                    return false;\n                }\n                else {\n                    const revertedPlaceholder = this.r.slice(0, this.r.length - historyHint.length);\n                    if (options.showPlaceholderOnFocus) {\n                        this.r = revertedPlacehol","startLine":474,"endLine":498,"chunkType":"block","mtime":1772878378008.3887}