{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/base/browser/ui/splitview/splitview.js","code":"S(lowPriorityIndex) {\n        const contentSize = this.n.reduce((r, i) => r + i.size, 0);\n        let emptyDelta = this.g - contentSize;\n        const indexes = $Ub(this.n.length - 1, -1);\n        const lowPriorityIndexes = indexes.filter(i => this.n[i].priority === 1 /* LayoutPriority.Low */);\n        const highPriorityIndexes = indexes.filter(i => this.n[i].priority === 2 /* LayoutPriority.High */);\n        for (const index of highPriorityIndexes) {\n            $1b(indexes, index);\n        }\n        for (const index of lowPriorityIndexes) {\n            $2b(indexes, index);\n        }\n        if (typeof lowPriorityIndex === 'number') {\n            $2b(indexes, lowPriorityIndex);\n        }\n        for (let i = 0; emptyDelta !== 0 && i < indexes.length; i++) {\n            const item = this.n[indexes[i]];\n            const size = $3i(item.size + emptyDelta, item.minimumSize, item.maximumSize);\n            const viewDelta = size - item.size;\n            emptyDelta -= viewDelta;\n           ","startLine":873,"endLine":895,"chunkType":"function","mtime":1772878378110.4668}