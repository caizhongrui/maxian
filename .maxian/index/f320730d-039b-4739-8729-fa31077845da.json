{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/base/parts/storage/common/storage.js","code":"async set(key, value, external = false) {\n        if (this.c === StorageState.Closed) {\n            return; // Return early if we are already closed\n        }\n        // We remove the key for undefined/null values\n        if ($Hg(value)) {\n            return this.delete(key, external);\n        }\n        // Otherwise, convert to String and store\n        const valueStr = $Ag(value) || Array.isArray(value) ? $0t(value) : String(value);\n        // Return early if value already set\n        const currentValue = this.f.get(key);\n        if (currentValue === valueStr) {\n            return;\n        }\n        // Update in cache and pending\n        this.f.set(key, valueStr);\n        this.j.set(key, valueStr);\n        this.h.delete(key);\n        // Event\n        this.b.fire({ key, external });\n        // Accumulate work by scheduling after timeout\n        return this.D();\n    }","startLine":133,"endLine":156,"chunkType":"function","mtime":1772878378528.3423}