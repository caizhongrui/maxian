{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/editor/common/model/textModelTokens.js","code":"h(deadline) {\n        // Read the time remaining from the `deadline` immediately because it is unclear\n        // if the `deadline` object will be valid after execution leaves this function.\n        const endTime = Date.now() + deadline.timeRemaining();\n        const execute = () => {\n            if (this.a || !this.b._textModel.isAttachedToEditor() || !this.k()) {\n                // disposed in the meantime or detached or finished\n                return;\n            }\n            this.j();\n            if (Date.now() < endTime) {\n                // There is still time before reaching the deadline, so yield to the browser and then\n                // continue execution\n                $E(execute);\n            }\n            else {\n                // The deadline has been reached, so schedule a new idle callback if necessary\n                this.g();\n            }\n        };\n        execute();\n    }","startLine":393,"endLine":414,"chunkType":"function","mtime":1772878380728.8506}