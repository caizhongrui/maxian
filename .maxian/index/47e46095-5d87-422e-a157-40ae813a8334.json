{"filePath":"/Users/caizhongrui/Documents/workspace/boyo/plugin/ide/src/tianhe-zhikai-ide/out-build/vs/code/electron-sandbox/processExplorer/processExplorerMain.js","code":"const selectionPids = this.n();\n                // If the selection does not contain the right clicked item, copy the right clicked\n                // item only.\n                if (!selectionPids?.includes(pid)) {\n                    selectionPids.length = 0;\n                    selectionPids.push(pid);\n                }\n                const rows = selectionPids?.map(e => $Y.document.getElementById(`pid-${e}`)).filter(e => !!e);\n                if (rows) {\n                    const text = rows.map(e => e.innerText).filter(e => !!e);\n                    this.c.writeClipboardText(text.join('\\n'));\n                }\n            }\n        });\n        items.push({\n            label: localize(170, null),\n            click: () => {\n                const processList = $Y.document.getElementById('process-list');\n                if (processList) {\n                    this.c.writeClipboardText(processList.innerText);\n                }\n            }\n        });\n        if (item && isLocal && thi","startLine":401,"endLine":425,"chunkType":"block","mtime":1772878379282.768}