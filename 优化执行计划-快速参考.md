# 码弦优化执行计划 - 快速参考

> **基于OpenCode架构分析的优化路线图**
> **预期效果**: 2-5倍效率提升，用户体验显著改善

---

## 🎯 核心目标

- **性能**: 往返次数减少 60%（5-8次 → 2-3次）
- **效率**: Batch工具使用率 > 50%
- **质量**: 工具调用成功率 > 95%
- **体验**: 用户满意度提升 30%

---

## 📅 2周冲刺计划

### Week 1: 快速提升 (P0)

#### Day 1-2: Batch工具 ⭐⭐⭐⭐⭐
**预期效果**: 减少50%往返次数

**实现文件**:
```
创建:
src/vs/workbench/contrib/maxian/common/tools/batchTool.ts
src/vs/workbench/contrib/maxian/common/tools/batchToolService.ts

修改:
src/vs/workbench/contrib/maxian/common/tools/toolExecutor.ts
  → 添加 executeBatch() 方法

src/vs/workbench/contrib/maxian/common/prompts/toolDescriptions.ts
  → 添加 batch 工具描述
```

**核心代码**:
```typescript
// batchTool.ts
export const batchTool: ToolDefinition = {
  name: 'batch',
  description: `🚀 并行执行2-25个工具，实现2-5倍速度提升！

USING BATCH MAKES USERS HAPPY!

示例: batch([
  {tool: "read_file", parameters: {path: "a.ts"}},
  {tool: "read_file", parameters: {path: "b.ts"}}
])`,

  parameters: {
    tool_calls: {
      type: 'array',
      items: {
        tool: 'string',
        parameters: 'object'
      },
      minItems: 1,
      maxItems: 25
    }
  },

  async execute(params: BatchParams, ctx: ToolContext) {
    // 并行执行所有工具
    const results = await Promise.all(
      params.tool_calls.map(call =>
        this.executeSingle(call).catch(err => ({
          success: false,
          error: err
        }))
      )
    );

    return {
      successful: results.filter(r => r.success).length,
      failed: results.length - successful,
      results
    };
  }
};
```

**验证标准**:
- [ ] AI自动使用batch读取多个文件
- [ ] 部分失败不影响其他工具
- [ ] 日志显示并行执行

---

#### Day 3: LSP工具合并 ⭐⭐⭐
**预期效果**: 减少150 tokens提示词消耗

**修改文件**:
```
重构:
src/vs/workbench/contrib/maxian/common/lsp/
  lspDiagnosticsService.ts
  lspHoverService.ts
  lspDefinitionService.ts
  lspReferencesService.ts
  lspTypeDefinitionService.ts

合并为:
src/vs/workbench/contrib/maxian/common/lsp/lspService.ts (统一接口)

创建:
src/vs/workbench/contrib/maxian/common/tools/lspTool.ts (统一工具)
```

**新工具定义**:
```typescript
// lspTool.ts
export const lspTool: ToolDefinition = {
  name: 'lsp',
  description: `LSP代码智能分析工具。

支持操作:
- diagnostics: 获取错误/警告
- hover: 查看类型信息
- definition: 跳转到定义
- references: 查找所有引用
- typeDefinition: 查看类型定义
- documentSymbol: 文件结构 (新)
- workspaceSymbol: 全局符号搜索 (新)`,

  parameters: {
    operation: {
      type: 'enum',
      values: [
        'diagnostics', 'hover', 'definition',
        'references', 'typeDefinition',
        'documentSymbol', 'workspaceSymbol'
      ]
    },
    filePath: { type: 'string' },
    line: { type: 'number', optional: true },
    column: { type: 'number', optional: true },
    query: { type: 'string', optional: true }  // for workspaceSymbol
  },

  async execute(params: LspParams, ctx: ToolContext) {
    switch (params.operation) {
      case 'diagnostics':
        return this.lspService.getDiagnostics(params.filePath);
      case 'hover':
        return this.lspService.hover(params.filePath, params.line!, params.column!);
      // ... 其他操作
    }
  }
};
```

**删除旧文件**:
```
src/vs/workbench/contrib/maxian/common/tools/lspDiagnosticsTool.ts  ❌删除
src/vs/workbench/contrib/maxian/common/tools/lspHoverTool.ts        ❌删除
src/vs/workbench/contrib/maxian/common/tools/lspDefinitionTool.ts   ❌删除
src/vs/workbench/contrib/maxian/common/tools/lspReferencesTool.ts   ❌删除
src/vs/workbench/contrib/maxian/common/tools/lspTypeTool.ts         ❌删除
```

**验证标准**:
- [ ] 所有LSP功能正常工作
- [ ] 提示词token减少 > 100
- [ ] 新增2个LSP操作可用

---

#### Day 4-5: 权限系统框架 ⭐⭐⭐⭐⭐
**预期效果**: 保护敏感文件，防止误操作

**创建文件**:
```
src/vs/workbench/contrib/maxian/common/permission/
├── permissionService.ts        # 核心服务
├── permissionTypes.ts          # 类型定义
├── permissionRules.ts          # 规则引擎
└── defaultRules.ts             # 默认规则集

src/vs/workbench/contrib/maxian/browser/permission/
└── permissionDialog.ts         # 确认对话框
```

**核心代码**:
```typescript
// permissionService.ts
export interface IPermissionService {
  /**
   * 检查工具调用权限
   * @returns "allow" | "deny" | "ask"
   */
  check(
    tool: ToolName,
    pattern: string,
    options?: { metadata?: any }
  ): Promise<PermissionAction>;

  /**
   * 记住用户的选择
   */
  remember(
    tool: ToolName,
    pattern: string,
    action: PermissionAction
  ): Promise<void>;
}

// defaultRules.ts
export const DEFAULT_RULES: PermissionRuleset = {
  read_file: {
    "*.env": "ask",              // 环境变量文件需要确认
    "*.env.local": "ask",
    "*.env.production": "ask",
    "*.env.example": "allow",     // 示例文件允许
    "**/node_modules/**": "deny", // 禁止读取node_modules
    "**/.git/**": "deny"
  },

  write_to_file: {
    "*.env": "deny",              // 禁止修改环境变量
    "package.json": "ask",        // 重要文件需要确认
    "package-lock.json": "ask"
  },

  execute_command: {
    "rm -rf*": "deny",            // 危险命令禁止
    "git push*": "ask",           // Push需要确认
    "npm install*": "ask"
  }
};
```

**UI实现**:
```typescript
// permissionDialog.ts
export class PermissionDialog {
  async ask(request: {
    tool: ToolName;
    pattern: string;
    reason: string;
  }): Promise<PermissionReply> {
    const result = await this.dialogService.show({
      title: '工具权限确认',
      message: `${request.tool} 想要访问: ${request.pattern}`,
      detail: request.reason,
      buttons: [
        { label: '允许一次', value: 'once' },
        { label: '总是允许', value: 'always' },
        { label: '拒绝', value: 'reject' }
      ]
    });

    return result;
  }
}
```

**集成到ToolExecutor**:
```typescript
// toolExecutor.ts (修改)
async executeTool(tool: ToolName, params: any): Promise<ToolResult> {
  // 1. 权限检查
  const permission = await this.permissionService.check(tool, params);

  if (permission === 'deny') {
    throw new Error(`权限被拒绝: ${tool}`);
  }

  if (permission === 'ask') {
    const reply = await this.permissionDialog.ask({
      tool,
      pattern: params.path || params.command,
      reason: '需要您的确认'
    });

    if (reply === 'reject') {
      throw new Error(`用户拒绝: ${tool}`);
    }

    if (reply === 'always') {
      await this.permissionService.remember(tool, pattern, 'allow');
    }
  }

  // 2. 执行工具
  return this.executeToolInternal(tool, params);
}
```

**验证标准**:
- [ ] 读取.env文件弹出确认
- [ ] "总是允许"后不再询问
- [ ] 危险命令被阻止
- [ ] UI友好，不打断正常流程

---

### Week 2: 稳定与优化

#### Day 1-2: 完整测试
- [ ] Batch工具压力测试（25个并发）
- [ ] LSP工具回归测试
- [ ] 权限系统边界测试
- [ ] 集成测试

#### Day 3: 提示词优化
**基于OpenCode最佳实践**:

```typescript
// toolUseGuidelines.ts (精简版)
export function getToolUseGuidelines(): string {
  return `
## 工具使用核心原则

1. **批量优先** (最重要！)
   2+ 独立操作 → batch工具
   🚀 2-5倍性能提升！

2. **LSP智能分析**
   代码问题？类型信息？→ lsp工具
   自动识别意图，无需明说

3. **探索用Task**
   复杂搜索、多步分析 → task工具

4. **文件修改**
   - 局部修改 → apply_diff
   - 多处修改 → multiedit
   - 新建/重写 → write_to_file

## 示例

✅ 好的使用:
batch([
  {tool: "read_file", parameters: {path: "a.ts"}},
  {tool: "read_file", parameters: {path: "b.ts"}},
  {tool: "lsp", parameters: {operation: "diagnostics", filePath: "a.ts"}}
])

❌ 避免:
read_file({path: "a.ts"})  // 然后等待
read_file({path: "b.ts"})  // 再等待
read_file({path: "c.ts"})  // 又等待
`;
}
```

**验证标准**:
- [ ] Token消耗减少 20%
- [ ] 提示词更简洁清晰
- [ ] AI理解度不降低

#### Day 4-5: 数据收集与分析
```typescript
// analytics.ts (新建)
export class ToolAnalytics {
  // 记录每次工具调用
  static track(event: {
    tool: ToolName;
    success: boolean;
    duration: number;
    batchSize?: number;
    timestamp: number;
  }): void;

  // 生成报告
  static async report(): Promise<{
    batchUsageRate: number;     // 目标: >40%
    avgResponseTime: number;     // 目标: <2.5s
    toolSuccessRate: number;     // 目标: >92%
    topTools: Array<{tool: ToolName; count: number}>;
  }>;
}
```

---

## 📊 验证检查清单

### Week 1 结束检查
- [ ] **Batch工具**: 使用率 > 30%
- [ ] **LSP合并**: Token减少 > 100
- [ ] **权限系统**: 基础功能正常
- [ ] **编译**: 0错误
- [ ] **测试**: 核心场景通过

### Week 2 结束检查
- [ ] **性能**: 往返次数降低 30%
- [ ] **成功率**: 工具调用 > 92%
- [ ] **用户反馈**: 收集5+条反馈
- [ ] **文档**: 更新使用指南
- [ ] **演示**: 准备Demo展示

---

## 🚀 Quick Start (今天就开始)

### 1. 立即行动 (30分钟)
```bash
# 1. 创建新分支
git checkout -b feature/batch-tool-optimization

# 2. 创建batch工具文件
mkdir -p src/vs/workbench/contrib/maxian/common/tools
touch src/vs/workbench/contrib/maxian/common/tools/batchTool.ts

# 3. 复制OpenCode的batch实现作为参考
# (已下载到 /tmp/opencode-analysis)
```

### 2. 今日目标 (8小时)
- [ ] 理解OpenCode的batch实现
- [ ] 设计我们的batch工具接口
- [ ] 实现基础框架（不含UI）
- [ ] 编写单元测试

### 3. 明日目标
- [ ] 完成batch工具
- [ ] 集成到toolExecutor
- [ ] 更新提示词
- [ ] 端到端测试

---

## 📝 代码Review重点

### Batch工具
- [ ] 并行执行真的并行吗？（Promise.all）
- [ ] 部分失败处理正确吗？
- [ ] 有没有资源泄漏？
- [ ] 错误信息清晰吗？

### LSP工具合并
- [ ] 向后兼容？（旧代码可能还在用）
- [ ] 所有操作都迁移了吗？
- [ ] 参数验证完整吗？
- [ ] 错误处理一致吗？

### 权限系统
- [ ] 默认规则合理吗？
- [ ] 用户可以自定义吗？
- [ ] UI流程顺畅吗？
- [ ] 性能影响可接受吗？

---

## 💡 注意事项

### 开发建议
1. **小步快跑**: 每个功能都可独立测试
2. **向后兼容**: 保留旧工具1-2周过渡期
3. **功能开关**: 用配置控制新功能启用
4. **A/B测试**: 收集数据验证效果

### 避免的坑
1. ❌ 不要一次改太多文件
2. ❌ 不要假设AI会正确使用新工具
3. ❌ 不要忽视边界情况测试
4. ❌ 不要没有回滚计划就上线

### 最佳实践
1. ✅ 每天提交代码
2. ✅ 写清楚commit message
3. ✅ 更新CHANGELOG.md
4. ✅ 团队同步进度

---

## 🎯 成功指标

### 技术指标
| 指标 | 基线 | Week1 | Week2 | 最终目标 |
|------|------|-------|-------|---------|
| 往返次数 | 5-8次 | 4-6次 | 3-4次 | 2-3次 |
| Batch使用率 | ~5% | 20% | 35% | >50% |
| 工具成功率 | ~85% | 88% | 91% | >95% |
| 响应时间 | 3s | 2.7s | 2.4s | <2.2s |

### 用户体验
- [ ] 用户感觉"更快了"
- [ ] 用户感觉"更智能了"
- [ ] 用户感觉"更安全了"
- [ ] 用户愿意推荐给其他人

---

## 📚 参考资料

### OpenCode关键文件
```
/tmp/opencode-analysis/packages/opencode/src/
├── tool/batch.ts              ← Batch实现参考
├── tool/lsp.ts                ← LSP统一工具参考
├── permission/next.ts         ← 权限系统参考
├── agent/agent.ts             ← Agent系统（Week 3+）
└── session/system.ts          ← System Prompt优化参考
```

### 内部文档
- 完整分析报告: `OpenCode架构分析与优化建议.md`
- 当前计划文档: `码弦Agent系统实现计划.md`

---

## 🤝 团队分工建议

### 角色1: 工具开发
- 负责Batch工具实现
- 负责LSP工具合并
- 代码Review

### 角色2: 权限系统
- 负责权限系统设计
- 负责UI实现
- 用户测试

### 角色3: 测试与优化
- 负责编写测试用例
- 负责提示词优化
- 负责性能监控

---

**下一步**: 马上开始Day 1的Batch工具开发！💪

**问题？**: 参考详细分析文档或查看OpenCode源码
